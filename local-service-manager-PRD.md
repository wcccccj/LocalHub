# 本地服务管理器 · 产品需求文档

> **项目名称**：LocalHub — 本地服务别名与快捷访问工具  
> **版本**：v1.0  
> **状态**：需求定稿  
> **更新日期**：2026-04-24

---

## 1. 背景与目标

### 1.1 问题描述

开发者在本地环境中通常同时运行多个服务，例如：

- **openclaw** 占用端口 7474（主服务）、7687（图数据库）
- **CPAMC** 占用端口 8443（管理面板）、9001（代理入口）
- 自定义开发服务（Node、Python、Nginx 等）

这些服务仅以"进程名 + 端口号"的形式存在，缺乏语义化标识，导致：

1. 难以快速识别某个端口对应的功能
2. 需要手动拼写 `http://localhost:PORT` 才能访问
3. 团队协作时无法共享统一的服务命名规范
4. 重启后需重新记忆端口映射关系

### 1.2 产品目标

构建一个**轻量级的本地服务管理工具**，实现：

- 自动扫描并展示当前正在监听的本地端口与进程
- 支持为每个服务配置**自定义别名**（类似域名/书签）和**备注说明**
- 提供**一键快速打开**服务的入口（浏览器直达）
- 配置持久化，重启后保留别名设置

---

## 2. 用户画像

| 角色 | 描述 | 核心需求 |
|------|------|----------|
| 独立开发者 | 同时运行多个本地服务 | 快速识别与访问，减少记忆负担 |
| 全栈工程师 | 前后端 + 数据库同时开启 | 按项目分组管理服务 |
| DevOps / 运维 | 管理多个代理、监控服务 | 状态监控 + 快速跳转 |
| 团队协作 | 共享本地服务配置 | 导出/导入配置，统一命名 |

---

## 3. 功能需求

### 3.1 核心功能（MVP）

#### F1 · 端口扫描与服务发现

- 工具启动时自动扫描本机所有监听端口（TCP/UDP）
- 识别并展示：端口号、关联进程名（PID）、协议类型
- 支持手动触发「重新扫描」刷新列表
- 区分服务状态：**运行中（Online）** / **已停止（Offline）**

**技术实现参考：**

```bash
# macOS / Linux 扫描命令
lsof -i -P -n | grep LISTEN

# 或使用 ss
ss -tlnp

# Windows
netstat -ano | findstr LISTENING
```

#### F2 · 服务别名管理

- 支持为每个端口/服务设置**自定义别名**（中英文均可）
- 别名格式建议：`项目名.功能` 或 `服务名-环境`，例如：
  - `openclaw.search`、`cpamc.admin`、`frontend.dev`
- 支持编辑**备注说明**（一行文字，描述服务用途）
- 别名修改实时保存，无需额外确认步骤

#### F3 · 快捷打开

- 对 HTTP/HTTPS/PROXY 类型服务，提供「打开」按钮
- 点击后在系统默认浏览器中打开 `http://localhost:{PORT}`
- 支持自定义协议前缀（`http://` vs `https://`）

#### F4 · 手动添加服务

- 支持手动录入服务（用于扫描未发现的固定端口）
- 录入字段：端口号、进程名、别名、协议类型、备注

#### F5 · 搜索与筛选

- 按别名、端口号、进程名进行实时搜索
- 按状态筛选：全部 / 运行中 / 已停止

---

### 3.2 进阶功能（v1.1+）

#### F6 · 分组管理

- 支持按「项目」对服务分组，例如将 openclaw 的两个端口归为一组
- 分组可折叠/展开

#### F7 · 配置导入导出

- 支持将别名配置导出为 JSON/YAML 文件
- 支持从文件导入，方便团队共享或跨设备同步

```json
{
  "services": [
    { "port": 7474, "alias": "OpenClaw 主服务", "type": "http", "note": "知识库引擎" },
    { "port": 8443, "alias": "CPAMC 管理面板", "type": "https", "note": "证书控制台" }
  ]
}
```

#### F8 · 状态变化通知

- 检测到已配置服务的状态变化（上线/下线）时，触发系统通知

#### F9 · 自定义本地域名（hosts 映射）

- 可选功能：将别名写入 `/etc/hosts`，实现通过 `http://openclaw.local` 直接访问
- 需要管理员权限，操作前需用户确认

---

## 4. 非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 端口扫描完成时间 < 2 秒 |
| 本地化 | 支持中英文界面切换 |
| 存储 | 配置文件存储于本地，不上传任何数据 |
| 权限 | 仅需普通用户权限扫描端口；hosts 修改需提权 |
| 兼容性 | 支持 macOS 13+、Windows 10+、Ubuntu 20.04+ |
| 安全 | 纯本地运行，无网络请求，无遥测 |

---

## 5. 信息架构

```
LocalHub
├── 服务列表（主视图）
│   ├── 统计卡片（总数 / 运行中 / 已停止）
│   ├── 搜索 + 状态筛选栏
│   └── 服务卡片列表
│       ├── 状态指示灯
│       ├── 别名（可编辑）
│       ├── 进程名 + 端口号
│       ├── 协议类型标签
│       ├── 备注（可编辑）
│       └── 操作：打开 / 编辑 / 删除
├── 手动添加服务（内联面板）
└── 设置
    ├── 扫描间隔
    ├── 开机自启
    └── 导入 / 导出配置
```

---

## 6. 数据模型

### 服务条目 `ServiceEntry`

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 唯一标识（UUID） |
| `port` | number | 监听端口号，1–65535 |
| `process` | string | 系统进程名（如 `node`、`python`） |
| `pid` | number \| null | 进程 ID（扫描获取） |
| `alias` | string | 用户自定义别名 |
| `note` | string | 备注说明 |
| `type` | enum | `http` \| `https` \| `tcp` \| `proxy` \| `db` \| `other` |
| `status` | enum | `online` \| `offline` |
| `isManual` | boolean | 是否手动添加（非扫描发现） |
| `group` | string \| null | 所属分组名（v1.1） |
| `updatedAt` | ISO 8601 | 最后编辑时间 |

---

## 7. 技术架构

### Electron 桌面应用

```
┌─────────────────────────────────┐
│         Electron Main           │
│  ┌──────────┐  ┌─────────────┐  │
│  │ Port     │  │  Config     │  │
│  │ Scanner  │  │  Manager   │  │
│  └──────────┘  └─────────────┘  │
├─────────────────────────────────┤
│         Renderer (React)        │
│   Service List UI + Edit UI     │
└─────────────────────────────────┘
```

- **Main Process**：调用系统命令（`lsof`/`netstat`）扫描端口
- **Renderer**：React + Tailwind，负责 UI 展示与交互
- **配置存储**：`electron-store`（JSON 持久化至用户数据目录）
- **IPC 通信**：`ipcMain` / `ipcRenderer` 传递扫描结果与配置变更

---

## 8. UI 设计规范

### 布局

- 主窗口尺寸：800 × 600 px（最小），支持缩放
- 服务卡片采用列表布局，每行一个服务
- 状态指示灯：绿色（在线）/ 红色（离线）

### 颜色语义

| 用途 | 颜色 |
|------|------|
| HTTP 类型标签 | 蓝色 `#185FA5` |
| TCP 类型标签 | 紫色 `#534AB7` |
| PROXY 类型标签 | 琥珀色 `#854F0B` |
| DB 类型标签 | 绿色 `#3B6D11` |
| 在线状态 | 绿色 `#1D9E75` |
| 离线状态 | 红色 `#A32D2D` |

### 端口号字体

端口号使用等宽字体（`monospace`）展示，与进程名区分层级。

---

## 9. 交互流程

### 主流程：首次启动

```
启动应用
    ↓
自动扫描本地监听端口
    ↓
展示发现的服务列表（含进程名、端口、状态）
    ↓
用户点击「编辑」→ 输入别名和备注 → 保存
    ↓
服务卡片展示别名
    ↓
用户点击「打开」→ 浏览器打开 localhost:PORT
```

### 二级流程：手动添加服务

```
点击「手动添加服务」
    ↓
展开添加面板（端口 / 进程名 / 别名 / 类型）
    ↓
填写信息 → 点击「添加」
    ↓
新条目加入服务列表
```

---

## 10. 验收标准

| 编号 | 场景 | 预期结果 |
|------|------|----------|
| AC-01 | 启动应用 | 2 秒内完成端口扫描，展示服务列表 |
| AC-02 | 编辑别名 | 输入别名后点击保存，刷新后别名持久保留 |
| AC-03 | 点击「打开」 | 系统浏览器打开 `http://localhost:{PORT}` |
| AC-04 | 服务下线 | 重新扫描后，已停止服务标记为红色离线状态 |
| AC-05 | 搜索 | 输入关键词实时过滤，匹配别名/端口/进程名 |
| AC-06 | 手动添加 | 填写端口后成功创建条目，不与已有条目冲突 |
| AC-07 | 删除服务 | 删除后条目从列表移除，配置文件同步更新 |
| AC-08 | 导出配置 | 生成合法 JSON 文件，包含所有别名与备注 |

---

## 11. 里程碑计划

| 阶段 | 内容 | 预计工期 |
|------|------|----------|
| M1 · 原型 | UI 原型（React 组件），模拟数据 | 2 天 |
| M2 · MVP | 端口扫描 + 别名配置 + 快速打开 | 5 天 |
| M3 · 稳定 | 配置持久化 + 状态刷新 + 搜索筛选 | 3 天 |
| M4 · 增强 | 分组 + 导入导出 + 系统通知 | 5 天 |
| M5 · 发布 | 打包分发（macOS `.dmg` / Win `.exe`）| 2 天 |

---

## 12. 参考与依赖

- [Electron](https://www.electronjs.org/) — 跨平台桌面应用框架
- [electron-store](https://github.com/sindresorhus/electron-store) — 本地配置持久化
- [node-portscanner](https://github.com/baalexander/node-portscanner) — 端口检测库
- [lsof](https://man7.org/linux/man-pages/man8/lsof.8.html) / [ss](https://man7.org/linux/man-pages/man8/ss.8.html) — 系统端口扫描命令

---

*本文档由 Claude 协助生成，最终实现细节以开发阶段确认为准。*
